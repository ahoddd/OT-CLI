{
  "name": "orbtap-functions",
  "version": "1.0.0",
  "description": "OrbTap Cloud Functions — Verify-Before-Create anti-bot email verification",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google-cloud/vision": "^5.0.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.0.5",
    "resend": "^4.0.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true
}
